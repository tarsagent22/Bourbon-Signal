// Retired permanently: syncing all members must never recreate marketing consent.
// Do not replace this with a dispatcher: retained scheduled callers are not approval.
console.error('Retired: legacy audience sync is disabled, including --apply. No contacts changed.');
process.exitCode = 1;
